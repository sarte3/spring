<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.finshot.web.mapper.EmpMapper">
	<insert id="write" parameterType="com.finshot.web.dto.EmpDto">
		insert into emp(eno, name, job, tel, email, updateday) values(#{eno}, #{name}, #{job}, #{tel}, #{email}, now())
	</insert>
	<select id="list" resultType="com.finshot.web.dto.EmpDto">
		select * from emp where ${param1} like '%'||#{param2}||'%' limit #{param3} offset #{param4}
	</select>
	<select id="getTotal" resultType="Integer">
		select count(*) from emp where ${param1} like '%'||#{param2}||'%'
	</select>
	<select id="content" resultType="com.finshot.web.dto.EmpDto">
		select * from emp where id::varchar=#{param1}
	</select>
	<insert id="update" parameterType="com.finshot.web.dto.EmpDto">
		update emp set eno=#{eno}, name=#{name}, job=#{job}, tel=#{tel}, email=#{email}, updateday=now() where id=#{id}
	</insert>
	<select id="checkEno" resultType="Integer">
		select count(*) from emp where eno=#{param1}
	</select>
	<delete id="delete" parameterType="com.finshot.web.dto.IdsDto">
		delete from emp where id::varchar in(${ids})
	</delete>
</mapper>